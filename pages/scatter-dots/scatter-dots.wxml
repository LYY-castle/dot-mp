<!--pages/scatter-dots/scatter-dots.wxml-->
<view class="container">
  <view class="head">
    <view class="title">撒点点</view>
    <van-image src="{{bg}}" alt fit="widthFix" width="100%"/>
    <view class="codeContent">
      <image src="{{avatar}}" alt class="avatar"/>
      <view class="card">
        <view class="m-b-20">{{ name }}</view>
        <van-image src="{{codeUrl}}" width="150px" height="150px"></van-image>
        <view class="font-size-12 m-t-20">[ 截屏转发金小点身份码 - 发展团队 ]</view>
      </view>
    </view>
  </view>
</view>
